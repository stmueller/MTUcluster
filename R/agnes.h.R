
# This file is automatically generated, you probably don't want to edit this

#' @importFrom jmvcore Options
#' @importFrom R6 R6Class
AgnesOptions <- R6::R6Class(
    "AgnesOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            vars = NULL,
            labels = NULL,
            distan = "euclidean",
            algo = "average",
            stand = FALSE, ...) {

            super$initialize(
                package='MTUcluster',
                name='Agnes',
                requiresData=TRUE,
                ...)
        
            private$..vars <- jmvcore::OptionVariables$new(
                "vars",
                vars)
            private$..labels <- jmvcore::OptionVariable$new(
                "labels",
                labels)
            private$..distan <- jmvcore::OptionList$new(
                "distan",
                distan,
                options=list(
                    "euclidean",
                    "maximum",
                    "manhattan",
                    "canberra",
                    "binary",
                    "minkowski"),
                default="euclidean")
            private$..algo <- jmvcore::OptionList$new(
                "algo",
                algo,
                options=list(
                    "single",
                    "complete",
                    "average",
                    "weighted",
                    "ward"),
                default="average")
            private$..stand <- jmvcore::OptionBool$new(
                "stand",
                stand,
                default=FALSE)
        
            self$.addOption(private$..vars)
            self$.addOption(private$..labels)
            self$.addOption(private$..distan)
            self$.addOption(private$..algo)
            self$.addOption(private$..stand)
        }),
    active = list(
        vars = function() private$..vars$value,
        labels = function() private$..labels$value,
        distan = function() private$..distan$value,
        algo = function() private$..algo$value,
        stand = function() private$..stand$value),
    private = list(
        ..vars = NA,
        ..labels = NA,
        ..distan = NA,
        ..algo = NA,
        ..stand = NA)
)

#' @import jmvcore
#' @importFrom R6 R6Class
AgnesResults <- R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        text = function() private$..text,
        plot = function() private$..plot,
        footer = function() private$..footer),
    private = list(
        ..text = NA,
        ..plot = NA,
        ..footer = NA),
    public=list(
        initialize=function(options) {
            super$initialize(options=options, name="", title="Hierarchical Clustering - Agglomerative")
            private$..text <- jmvcore::Preformatted$new(
                options=options,
                name="text",
                title="Hierarchical Clustering - Agglomerative")
            private$..plot <- jmvcore::Image$new(
                options=options,
                name="plot",
                title="Dendrogram Plot",
                width=400,
                height=500,
                renderFun=".plot")
            private$..footer <- jmvcore::Preformatted$new(
                options=options,
                name="footer",
                title="About")
            self$add(private$..text)
            self$add(private$..plot)
            self$add(private$..footer)}))

#' @importFrom jmvcore Analysis
#' @importFrom R6 R6Class
AgnesBase <- R6::R6Class(
    "AgnesBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'MTUcluster',
                name = 'Agnes',
                version = c(1,0,0),
                options = options,
                results = AgnesResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE)
        }))

#' Hierarchical Clustering - Agglomerative
#'
#' 
#' @param data .
#' @param vars The variables to use.
#' @param labels .
#' @param distan .
#' @param algo .
#' @param stand .
#' @export
Agnes <- function(
    data,
    vars,
    labels,
    distan = "euclidean",
    algo = "average",
    stand = FALSE) {

    options <- AgnesOptions$new(
        vars = vars,
        labels = labels,
        distan = distan,
        algo = algo,
        stand = stand)

    results <- AgnesResults$new(
        options = options)

    analysis <- AgnesClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}
